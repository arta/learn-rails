<% content_for :title do %>Contact<% end %>
<h3>Contact</h3>
<h4>done the Rails way, following the `web app paradigm`</h4>
<h4>(URL goes to router where the request's controller and action are parsed out of it and nested into params as params[:controller]='contacts', and params[:action]='new')</h4>
<div class="form">
  <%= simple_form_for @contact do |form| %>
    <%= form.error_notification %>
    <%= form.input :name, autofocus: true %>
    <%= form.input :email %>
    <%= form.input :content, as: :text %>
    <%= form.button :submit, 'Submit', class: 'submit' %>
  <% end %>
</div>
